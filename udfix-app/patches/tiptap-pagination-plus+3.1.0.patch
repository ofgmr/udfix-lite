diff --git a/node_modules/tiptap-pagination-plus/dist/PaginationPlus.js b/node_modules/tiptap-pagination-plus/dist/PaginationPlus.js
index 186b675..4fe4112 100644
--- a/node_modules/tiptap-pagination-plus/dist/PaginationPlus.js
+++ b/node_modules/tiptap-pagination-plus/dist/PaginationPlus.js
@@ -523,12 +523,20 @@ const getExistingPageCount = (view) => {
     }
     return 0;
 };
+/** NOMAI: prevent runaway page widgets + view.update dispatch storm */
+const NOMAI_MAX_TOTAL_PAGES = 500;
+const NOMAI_MAX_ADD_PAGES_PER_PASS = 24;
 const calculatePageCount = (view, pageOptions, headerHeight = 0, footerHeight = 0) => {
     var _a;
     const editorDom = view.dom;
-    const _pageHeaderHeight = pageOptions.contentMarginTop + pageOptions.marginTop + headerHeight;
-    const _pageFooterHeight = pageOptions.contentMarginBottom + pageOptions.marginBottom + footerHeight;
-    const pageContentAreaHeight = pageOptions.pageHeight - _pageHeaderHeight - _pageFooterHeight;
+    const domHeaderEl = editorDom.querySelector(".rm-first-page-header");
+    const domFooterEl = editorDom.querySelector("[data-rm-pagination] .rm-page-footer") || editorDom.querySelector(".rm-page-footer");
+    const hDom = domHeaderEl ? domHeaderEl.clientHeight : 0;
+    const fDom = domFooterEl ? domFooterEl.clientHeight : 0;
+    const _pageHeaderHeight = pageOptions.contentMarginTop + pageOptions.marginTop + Math.max(headerHeight, hDom);
+    const _pageFooterHeight = pageOptions.contentMarginBottom + pageOptions.marginBottom + Math.max(footerHeight, fDom);
+    const rawArea = pageOptions.pageHeight - _pageHeaderHeight - _pageFooterHeight;
+    const pageContentAreaHeight = Math.max(80, Number.isFinite(rawArea) ? rawArea : 80);
     const paginationElement = editorDom.querySelector("[data-rm-pagination]");
     const currentPageCount = getExistingPageCount(view);
     if (paginationElement) {
@@ -540,28 +548,29 @@ const calculatePageCount = (view, pageOptions, headerHeight = 0, footerHeight =
             const lastPageGap = lastElementRect.bottom -
                 lastPageBreakRect.bottom;
             if (lastPageGap > 0) {
-                const addPage = Math.ceil(lastPageGap / pageContentAreaHeight);
-                return currentPageCount + addPage;
+                let addPage = Math.ceil(lastPageGap / pageContentAreaHeight);
+                addPage = Math.min(NOMAI_MAX_ADD_PAGES_PER_PASS, addPage);
+                return Math.min(NOMAI_MAX_TOTAL_PAGES, currentPageCount + addPage);
             }
             else {
                 const allBreaksAfterLastElement = Array.from(paginationElement.querySelectorAll(".breaker"));
                 const allBreaksAfterLastElementRect = allBreaksAfterLastElement.filter(element => element.getBoundingClientRect().top > lastElementRect.bottom);
                 const removePage = allBreaksAfterLastElementRect.length;
                 if (removePage > 1) {
-                    return currentPageCount - removePage;
+                    return Math.min(NOMAI_MAX_TOTAL_PAGES, currentPageCount - removePage);
                 }
                 else {
-                    return currentPageCount;
+                    return Math.min(NOMAI_MAX_TOTAL_PAGES, currentPageCount);
                 }
             }
         }
-        return 1;
+        return Math.min(NOMAI_MAX_TOTAL_PAGES, 1);
     }
     else {
         const editorHeight = editorDom.scrollHeight;
         let pageCount = Math.ceil(editorHeight / pageContentAreaHeight);
         pageCount = pageCount <= 0 ? 1 : pageCount;
-        return pageCount;
+        return Math.min(NOMAI_MAX_TOTAL_PAGES, pageCount);
     }
 };
 const getNewPageCount = (view, pageOptions) => {
diff --git a/node_modules/tiptap-pagination-plus/dist/utils.js b/node_modules/tiptap-pagination-plus/dist/utils.js
index 8ee009b..9f78940 100644
--- a/node_modules/tiptap-pagination-plus/dist/utils.js
+++ b/node_modules/tiptap-pagination-plus/dist/utils.js
@@ -170,7 +170,9 @@ export function getHeader(headerRightContent, headerLeftContent, onHeaderClick,
 export const getHeight = (pageOptions, _headerHeight, _footerHeight) => {
     const _pageHeaderHeight = pageOptions.contentMarginTop + pageOptions.marginTop + _headerHeight;
     const _pageFooterHeight = pageOptions.contentMarginBottom + pageOptions.marginBottom + _footerHeight;
-    const _pageHeight = pageOptions.pageHeight - _pageHeaderHeight - _pageFooterHeight;
+    const raw = pageOptions.pageHeight - _pageHeaderHeight - _pageFooterHeight;
+    /** NOMAI: negative/zero blows up calculatePageCount (ceil(lastGap / 0) → Infinity pages). */
+    const _pageHeight = Math.max(80, Number.isFinite(raw) ? raw : 80);
     return {
         _pageHeaderHeight,
         _pageFooterHeight,
